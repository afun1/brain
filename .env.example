# Database Configuration
# PostgreSQL connection string
DATABASE_URL=postgresql://user:password@localhost:5432/brain_entrainer

# Session Configuration
# Generate a secure random string for production (min 32 characters)
# You can use: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=change-this-to-a-secure-random-string-in-production

# OpenAI Configuration (for TTS and Translation features)
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-api-key-here

# Application Configuration
NODE_ENV=development
PORT=5000

# Replit Integration (only needed if deploying on Replit)
REPLIT_DEPLOYMENT=false
# REPLIT_DB_URL=your-replit-db-url

# Optional: External Services
# Add any additional API keys or service configurations here
